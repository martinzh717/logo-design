---
name: logo-design
slug: logo-design
displayName: Logo 设计与交付
version: 1.0.2
summary: 按专业流程做可交付 Logo：需求书、方向、矢量定稿、应用测试与文件包；有无生图能力均可
license: MIT
tags:
  - logo
  - 标志
  - 徽标
  - 品牌
  - SVG
  - 设计
description: >-
  指导 Logo / 标志 / 徽标从需求确认、视觉方向、矢量定稿、应用测试、使用规范到文件交付的完整流程。
  默认以可编辑 SVG 为母版，没有生图能力的 Agent 也能做完；若有生图工具仅用于概念探索，必须重建为矢量。
  适用于做 Logo、字标、字母标、图形标、App 图标、品牌标识、VI 锁定稿、Logo 使用规范与交付包。
  不适用于整站官网、动效站点或纯插画（请用对应技能）。
---

# Logo 设计与交付

自包含工作流。目标不是「生成一张好看的图」，而是交出**可识别、可缩小、可改、能上线**的标志。

**主文件永远是矢量（SVG）。** 栅格图只用于探索、预览或规定场景导出。没有生图模型、没有商业绘图 API 时，流程仍然完整。

## 原则

**需求 → 差异 → 一个图形概念 → 黑白成立 → 配色 → 应用测试 → 规范与交付。**

- 形容词（大气、高级、温暖）在变成图形规则之前，不算设计决定。
- 先看黑白形态是否清晰，再上色。颜色救不了一个含糊的外形。
- 一个标志只讲一个识别点。细节越多，头像尺寸越先死。
- 同类组织长什么样，决定你要避开什么，而不是抄什么。
- 栅格探索稿必须重建为路径；PNG/WebP 不能当母版。

## 能力路由（先探测，再选轨）

会话开始时判断一次。**缺生图能力不是失败**，不要停下来要求用户换模型。

| 轨 | 条件 | 本轮必须产出 |
|---|---|---|
| **A 矢量主轨（默认、必做）** | 能写文件 | SVG 母版 + HTML 测试页 + 规范说明 |
| **B 栅格探索（可选）** | 存在生图/改图工具，且用户要看图 | 2–4 张概念图，**随后用轨 A 重建** |
| **C 规格轨** | 不能写文件，或用户只要 brief/规范 | 填好的 Brief、方向卡、绘制说明，交给人或下一轮 Agent |

探测顺序：

1. 能否写入工作区文件？不能 → 轨 C。
2. 是否有生图/改图工具（任意一家即可）？有且用户要看图 → 轨 B 辅助轨 A；否则只走轨 A。
3. 是否有 SVG→PNG 转换（浏览器导出、`rsvg-convert`、设计软件）？没有 → 交付 SVG + 测试 HTML，并写明 PNG 由使用方导出。

禁止：因为没有 Midjourney / Flux / Cursor GenerateImage 就拒绝做 Logo。禁止把某一家工具写成硬依赖。

细节见 [rendering.md](rendering.md)。

## 九阶段

按编号交接。小项目可合并，但 **1、7、9 不可删**。

| 阶段 | 动作 | 交付 | 完成判断 |
|---|---|---|---|
| 1 需求确认 | 名称、受众、场景、禁区、决策人 | Brief | 决策人、初稿数量、修改轮次已写明 |
| 2 调研 | 品牌关键词 vs 同类视觉 | 对照结论 | 写清「我们要被记住的一点」 |
| 3 视觉方向 | 字标 / 图形标 / 组合；字体与造型倾向 | 方向板 | 形式已选，风格可执行 |
| 4 草图 | 多个粗概念，黑白优先 | 草图或 SVG 草稿 | 至少 3 个**结构不同**的概念 |
| 5 提案 | 同尺寸、同场景比较；放出无装饰本体 | 方案页 | 每个方案能解释与品牌的关系 |
| 6 定稿 | 围绕一个方向做比例、字距、颜色 | 定稿 SVG | 意见已分成必须改 / 偏好 |
| 7 应用测试 | 小尺寸、圆裁、单色、深浅底 | 测试页 | 失败项已修正，不是「以后再说」 |
| 8 使用规范 | 留白、最小尺寸、色值、禁用 | 简版手册 | 别人按手册做物料不会走形 |
| 9 交付上线 | 源文件、版本、授权记录 | 文件包 | 能打开、能用、命名清楚 |

**四个确认节点：** 需求确认 → 方向确认 → 定稿确认 → 交付验收。未过节点不要假装已进入下一阶段。

## Agent 操作规则

1. **先定阶段。** Brief 空着时不要出精致彩色 Logo。缺决策人、使用场景或名称时，问一轮就够，用 [templates.md](templates.md) 填。
2. **概念必须结构不同。** 三个方案只换颜色或只换圆角，算未完成阶段 4。
3. **先黑白 SVG，再配色。** 构造用几何与字重，不用阴影、描边光、照片拼贴。
4. **有生图工具时：** 只用来发散或做情绪板；选定方向后按 [craft.md](craft.md) 手写/编码 SVG。提示词见 [rendering.md](rendering.md)。
5. **宣称完成前**走 [delivery.md](delivery.md) 验收清单。没有测试页不算交付；测试页必须内联 SVG（见 [rendering.md](rendering.md)），避免 IDE 预览裂图。
6. **字体与素材要可交代来源。** 不能确认授权的字体，改用开源字体或完全自绘字母。

### 禁止（AI Logo 常见翻车）

- 渐变球体、玻璃拟态、3D 挤出、无限符号、 swoosh、泛紫霓虹
- 在标志里塞口号、网址、二维码
- 细线密纹、照片、复杂插画当主标
- 拉伸、描边充面积、随便改色当「响应式」
- 把生图 PNG 当最终文件
- 用「感觉更高级了」代替可执行修改

工艺细则：[craft.md](craft.md)。

## 按需阅读

| 需要 | 阅读 |
|---|---|
| Brief、方向卡、评审反馈 | [templates.md](templates.md) |
| 形态、字标、色彩、避坑 | [craft.md](craft.md) |
| 有无生图、如何写 SVG、如何导出 | [rendering.md](rendering.md) |
| 测试页、文件包、验收 | [delivery.md](delivery.md) |
| 端到端假想案例 | [examples.md](examples.md) |

## 公开方法来源（可选）

均为公开资料；本 skill 不依赖本地项目路径或私有工具。

- Adobe Express — How to design a logo（阶段划分、应用测试、使用规范）
- 99designs — Project brief、project stages、final files to deliver

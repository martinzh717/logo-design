# 测试、规范与交付

## 应用测试（阶段 7，不可跳过）

用 `preview.html` 或同等画板，按**真实用途**测，不要虚构一个通用像素门槛当所有项目的标准。

| 场景 | 重点 |
|---|---|
| 社交头像与圆形裁切 | 主体是否被切掉；32px 是否还能认 |
| 网站 favicon / 标签栏 | 16px 是否只剩墨点 |
| 海报与文档页脚 | 名称是否可读；与其他标识并排是否抢 |
| 黑白打印 / 单色复印 | 去掉颜色后结构是否成立 |
| 深色底 / 衣服反白 | 是否有合格单白文件，而非临时把彩色滤成灰 |
| 门头、胸牌等实物 | 按实际尺寸看细线与字怀（能打样则打样） |

失败则回到阶段 6 改形，不要用投影或描边硬撑。

## 使用规范（简版就够）

`usage.md` 至少包含：

```text
品牌名与标志版本列表（日期）
主色 / 辅色 hex（及已知的印刷备注）
最小尺寸（图形宽或字高，按实际测试填写）
保护空间（例如等于图形中某个重复模块的边长）
背景：浅 / 深 / 摄影上的规则（摄影上优先放在干净区域或加纯色底）
禁止：拉伸、旋转成装饰、改色、加阴影、拆散字母、把口号塞进主标、低分辨率拉大
文件清单与每个文件干什么
字体名称、来源、授权方式
```

## 建议交付包

| 内容 | 说明 |
|---|---|
| 可编辑源 | SVG；若用设计软件另存，注明软件名。没有 `.ai` 不算缺陷 |
| 彩色 / 单黑 / 单白 | 分文件 |
| 横版、竖版、纯图形 | **按 Brief 里确认的场景给**，不机械凑齐 |
| PNG（若已能导出） | 透明底；常用 512 / 1024；图标可另给圆角版 |
| PDF/EPS | 仅当印刷方指定；否则 SVG + PDF 打印预览即可 |
| preview.html | 测试页；**SVG 须内联**，打开即可见（见 rendering.md） |
| usage.md + brief.md | 规范与需求 |
| 字体与素材授权记录 | 来源不明则不要进最终包 |

上线前让**实际使用者**打开文件；正式印刷先看打样。

## 验收清单

- [ ] 名称、简称、字母拼写正确
- [ ] 设计能用一句话对上 Brief 里的差异点
- [ ] 至少 3 个结构不同概念被探索；定稿只保留一个方向
- [ ] 黑白成立后再上色
- [ ] 小尺寸、圆裁、单色、深浅底已测并修正
- [ ] `preview.html` 内联 SVG，不依赖相对路径 `img`；深浅底为独立着色而非仅靠 CSS invert
- [ ] 母版为 SVG，可打开，无外链图、无未知字体依赖（或已声明并附字体）
- [ ] 彩色 / 深底 / 单色版本按约定给齐
- [ ] 使用规范写清留白、最小尺寸、禁止项
- [ ] 字体与素材授权可交代
- [ ] 决策人已确认；文件有统一目录与日期

**完成标准：图形定稿、实际可用、文件齐全、使用规则清楚。**

未完成时禁止说「Logo 已做好」。应列出缺的节点或失败的测试项。
